
export interface QualifyingResults {
    qualifyID: number;
    raceID: number;
    driverID: number;
    constructorID: number;
    q1Time: string; // Assuming TIME format as a string
    q2Time: string; // Assuming TIME format as a string
    q3Time: string; // Assuming TIME format as a string
    gridPosition: number;
}
